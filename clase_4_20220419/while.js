/**
 * Ciclos son secuencias repetitivas 
 */

  /*
  * while (condición)
    expresión
  */
  const prompt = require('prompt');
  prompt.start();
  
  let estado = "Encendido";
  while(estado === "Encendido") {
    console.log("Estado " + estado)
    console.log("Estado " + estado)
    console.log("Estado " + estado)
    console.log("Estado " + estado)
    estado = "Apagado"
  }

